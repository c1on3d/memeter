# Server Configuration
PORT=8080
NODE_ENV=development

# Cloud SQL Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=memeter

# For Cloud SQL Socket (production)
# INSTANCE_CONNECTION_NAME=your-project:region:instance-name

# PumpPortal WebSocket
PUMPPORTAL_WS_URL=wss://pumpportal.fun/api/data

# PumpAPI.io WebSocket (alternative pump.fun data source)
PUMPAPI_WS_URL=wss://stream.pumpapi.io/

# Helius API (get key from https://www.helius.dev/)
HELIUS_API_KEY=your_helius_api_key_here

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
